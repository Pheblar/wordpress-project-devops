# Пример файла с секретами. НЕ коммитить в git.
# Использование: helm install -f values.yaml -f values-dev.yaml -f values-secrets.yaml ...
# Или передать через --set:
#   --set mysqlRootPassword=xxx --set mysqlPassword=yyy

mysqlRootPassword: "your-root-password"
mysqlPassword: "your-wp-user-password"
